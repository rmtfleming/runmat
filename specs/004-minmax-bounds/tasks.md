# Tasks — 004-minmax-bounds

- [ ] **T1** Inspect `min.rs`/`max.rs` for reusable reduction helpers and dim
  semantics; record whether a visibility change is needed. [FR-004-03]
- [ ] **T2** Author failing tests: three observed cases (normative), hi
  (`summary_derived`), NaN/empty/int32 (`unresolved_choice`).
  [FR-004-01..05; SC-004-1]
- [ ] **T3** Implement `bounds.rs` (delegating reduction, two outputs,
  descriptor, registration). [FR-004-01..04]
- [ ] **T4** Verification commands; `validation.md` with traceability table.
  [SC-004-1..3]
