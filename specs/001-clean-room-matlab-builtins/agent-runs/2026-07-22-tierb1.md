# Agent Run Ledger — 2026-07-22 — Tier B batch 1 (import + features 016–021)

**Instruction**: maintainer directed copy of the new approved specs and to
get started.

## Import
- 12 approved Tier B batch-1 specs copied from source commit
  `e46587ae1b78237e0f43cd55f678de01a28495a4` (verified clean at HEAD for all
  12; source working tree holds unrelated draft batch-2 files, excluded).
- Approval record: source `docs/review/2026-07-22-tier-b-batch1-human-loop.md`.
- Byte-identical to source; SHA256SUMS regenerated (38 specs).
- Committed: import + FR-010 proposal.

## Gate (FR-010) — RECORDED 2026-07-22
- Six features authorised: F16 (isrow, iscolumn, issparse, iscellstr,
  istable-as-always-false), F17 nonzeros, F18 dec2bin, F19 computer,
  F20 strncmpi, F21 psi.
- istable: implement now as always-false (no table type; true-case
  unreachable), documented.
- addvars, removevars: BLOCKED with the table cluster (010).

## Implementation
- Six parallel agents (full B1–B5 per feature), bound to constitution,
  imports, and batch-1/2 template files. Commit-pinned provenance
  (e46587a).

## Outcome (verified by main session)

- All six agents completed green. Main-session re-verification:
  `cargo check` clean; **111/111** new-builtin tests (F16 31, F17 13,
  F18 16, F19 12, F20 21, F21 18); **6765/6765** full runtime lib suite
  (zero regressions); clippy clean; `cargo fmt --all --check` clean.
- 12 builtins implemented: isrow, iscolumn, issparse, iscellstr, istable
  (F16); nonzeros (F17); dec2bin (F18); computer (F19); strncmpi (F20);
  psi (F21). `addvars`/`removevars` remain table-blocked.
- Highlight: `psi` digamma matches observed values to ~1 ulp (delta ~1e-16),
  Bernoulli asymptotic series sourced to Abramowitz & Stegun (Principle X).
- No MATLAB invocation; matlab-interface-spec untouched.
